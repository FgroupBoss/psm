<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.risk.inspection.mapper.SignRecordMapper">

    <select id="listByTask" resultType="com.fgroupboss.ai.psm.risk.inspection.model.entity.SignRecordEntity">
        select * from insp_sign_record
        where tenant_id = #{tenantId}
          and task_id = #{taskId}
        order by signed_at asc
    </select>

    <select id="findByTaskAndPoint" resultType="com.fgroupboss.ai.psm.risk.inspection.model.entity.SignRecordEntity">
        select * from insp_sign_record
        where tenant_id = #{tenantId}
          and task_id = #{taskId}
          and route_point_id = #{routePointId}
        limit 1
    </select>
</mapper>
