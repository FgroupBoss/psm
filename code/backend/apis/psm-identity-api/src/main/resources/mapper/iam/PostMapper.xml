<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.identity.iam.mapper.PostMapper">

    <sql id="columns">
        id, tenant_id, post_code, post_name, org_id, status, created_at, updated_at, deleted
    </sql>

    <select id="findById" resultType="com.fgroupboss.ai.psm.identity.iam.model.entity.PostEntity">
        select <include refid="columns"/>
        from sys_post
        where id = #{id}
          and tenant_id = #{tenantId}
          and deleted = 0
    </select>

    <select id="findByCode" resultType="com.fgroupboss.ai.psm.identity.iam.model.entity.PostEntity">
        select <include refid="columns"/>
        from sys_post
        where tenant_id = #{tenantId}
          and post_code = #{postCode}
          and deleted = 0
    </select>

    <select id="list" resultType="com.fgroupboss.ai.psm.identity.iam.model.entity.PostEntity">
        select <include refid="columns"/>
        from sys_post
        where tenant_id = #{tenantId}
          and deleted = 0
        <if test="keyword != null and keyword != ''">
          and (post_code like concat('%', #{keyword}, '%') or post_name like concat('%', #{keyword}, '%'))
        </if>
        order by id desc
        limit #{limit} offset #{offset}
    </select>

    <select id="count" resultType="long">
        select count(1)
        from sys_post
        where tenant_id = #{tenantId}
          and deleted = 0
        <if test="keyword != null and keyword != ''">
          and (post_code like concat('%', #{keyword}, '%') or post_name like concat('%', #{keyword}, '%'))
        </if>
    </select>

    <update id="updatePost">
        update sys_post
        set post_name = #{postName},
            org_id = #{orgId},
            status = #{status},
            updated_at = now()
        where id = #{id}
          and tenant_id = #{tenantId}
          and deleted = 0
    </update>

    <update id="softDelete">
        update sys_post
        set deleted = 1,
            updated_at = now()
        where id = #{id}
          and tenant_id = #{tenantId}
          and deleted = 0
    </update>
</mapper>
