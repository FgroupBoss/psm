<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.iam.mapper.RoleMapper">

    <sql id="columns">
        id, tenant_id, role_code, role_name, role_type, description, status, created_at, updated_at, deleted
    </sql>

    <select id="findById" resultType="com.fgroupboss.ai.psm.iam.model.entity.RoleEntity">
        select <include refid="columns"/>
        from sys_role
        where id = #{id}
          and tenant_id = #{tenantId}
          and deleted = 0
    </select>

    <select id="findByCode" resultType="com.fgroupboss.ai.psm.iam.model.entity.RoleEntity">
        select <include refid="columns"/>
        from sys_role
        where tenant_id = #{tenantId}
          and role_code = #{roleCode}
          and deleted = 0
    </select>

    <select id="list" resultType="com.fgroupboss.ai.psm.iam.model.entity.RoleEntity">
        select <include refid="columns"/>
        from sys_role
        where tenant_id = #{tenantId}
          and deleted = 0
        <if test="keyword != null and keyword != ''">
          and (role_code like concat('%', #{keyword}, '%') or role_name like concat('%', #{keyword}, '%'))
        </if>
        order by id desc
        limit #{limit} offset #{offset}
    </select>

    <select id="count" resultType="long">
        select count(1)
        from sys_role
        where tenant_id = #{tenantId}
          and deleted = 0
        <if test="keyword != null and keyword != ''">
          and (role_code like concat('%', #{keyword}, '%') or role_name like concat('%', #{keyword}, '%'))
        </if>
    </select>

    <update id="updateRole">
        update sys_role
        set role_name = #{roleName},
            role_type = #{roleType},
            description = #{description},
            status = #{status},
            updated_at = now()
        where id = #{id}
          and tenant_id = #{tenantId}
          and deleted = 0
    </update>

    <update id="softDelete">
        update sys_role
        set deleted = 1,
            updated_at = now()
        where id = #{id}
          and tenant_id = #{tenantId}
          and deleted = 0
    </update>
</mapper>
