<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.iam.mapper.UserRoleMapper">

    <delete id="deleteByUser">
        delete from sys_user_role
        where tenant_id = #{tenantId}
          and user_id = #{userId}
    </delete>

    <select id="findRoleIdsByUser" resultType="long">
        select role_id
        from sys_user_role
        where tenant_id = #{tenantId}
          and user_id = #{userId}
    </select>

    <select id="findUserIdsByRole" resultType="long">
        select user_id
        from sys_user_role
        where tenant_id = #{tenantId}
          and role_id = #{roleId}
    </select>
</mapper>
