<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.identity.iam.mapper.TenantMapper">

    <sql id="columns">
        id, tenant_code, tenant_name, tenant_type, status, admin_user_id, data_isolation_mode,
        created_at, updated_at, deleted
    </sql>

    <select id="findById" resultType="com.fgroupboss.ai.psm.identity.iam.model.entity.TenantEntity">
        select <include refid="columns"/>
        from sys_tenant
        where id = #{id}
          and deleted = 0
    </select>

    <select id="findByCode" resultType="com.fgroupboss.ai.psm.identity.iam.model.entity.TenantEntity">
        select <include refid="columns"/>
        from sys_tenant
        where tenant_code = #{tenantCode}
          and deleted = 0
    </select>

    <select id="list" resultType="com.fgroupboss.ai.psm.identity.iam.model.entity.TenantEntity">
        select <include refid="columns"/>
        from sys_tenant
        where deleted = 0
        <if test="keyword != null and keyword != ''">
          and (tenant_code like concat('%', #{keyword}, '%') or tenant_name like concat('%', #{keyword}, '%'))
        </if>
        order by id desc
        limit #{limit} offset #{offset}
    </select>

    <select id="count" resultType="long">
        select count(1)
        from sys_tenant
        where deleted = 0
        <if test="keyword != null and keyword != ''">
          and (tenant_code like concat('%', #{keyword}, '%') or tenant_name like concat('%', #{keyword}, '%'))
        </if>
    </select>

    <update id="updateTenant">
        update sys_tenant
        set tenant_name = #{tenantName},
            tenant_type = #{tenantType},
            status = #{status},
            admin_user_id = #{adminUserId},
            data_isolation_mode = #{dataIsolationMode},
            updated_at = now()
        where id = #{id}
          and deleted = 0
    </update>

    <update id="updateStatus">
        update sys_tenant
        set status = #{status},
            updated_at = now()
        where id = #{id}
          and deleted = 0
    </update>
</mapper>
