<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.auth.mapper.IdentityProviderMapper">

    <sql id="columns">
        id, tenant_id, provider_code, provider_type, provider_name, client_id, client_secret,
        authorize_url, token_url, user_info_url, callback_url, user_mapping_field, config, enabled
    </sql>

    <select id="listEnabled" resultType="com.fgroupboss.ai.psm.auth.model.entity.IdentityProviderEntity">
        select <include refid="columns"/>
        from auth_identity_provider
        where tenant_id = #{tenantId}
          and enabled = 1
        order by id asc
    </select>

    <select id="findEnabled" resultType="com.fgroupboss.ai.psm.auth.model.entity.IdentityProviderEntity">
        select <include refid="columns"/>
        from auth_identity_provider
        where tenant_id = #{tenantId}
          and provider_code = #{providerCode}
          and enabled = 1
    </select>
</mapper>
