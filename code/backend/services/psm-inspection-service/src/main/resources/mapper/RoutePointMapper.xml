<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.inspection.mapper.RoutePointMapper">

    <select id="listByRoute" resultType="com.fgroupboss.ai.psm.inspection.model.entity.RoutePointEntity">
        select * from insp_route_point
        where tenant_id = #{tenantId}
          and route_id = #{routeId}
          and deleted = 0
        order by sort_order asc, id asc
    </select>

    <select id="findByIdAndRoute" resultType="com.fgroupboss.ai.psm.inspection.model.entity.RoutePointEntity">
        select * from insp_route_point
        where tenant_id = #{tenantId}
          and route_id = #{routeId}
          and id = #{pointId}
          and deleted = 0
        limit 1
    </select>

    <update id="softDeleteByRoute">
        update insp_route_point
        set deleted = 1, updated_at = now()
        where tenant_id = #{tenantId}
          and route_id = #{routeId}
          and deleted = 0
    </update>
</mapper>
