<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.contractor.mapper.ContractorBlacklistMapper">

    <select id="findActiveByTarget" resultType="com.fgroupboss.ai.psm.contractor.model.entity.ContractorBlacklistEntity">
        select * from contractor_blacklist
        where tenant_id = #{tenantId}
          and target_type = #{targetType}
          and target_id = #{targetId}
          and status = 'ACTIVE'
          and deleted = 0
        limit 1
    </select>
</mapper>
