<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fgroupboss.ai.psm.video.mapper.VideoAiEventMapper">

    <sql id="eventWhere">
        tenant_id = #{tenantId}
        and deleted = 0
        <if test="cameraId != null">
            and camera_id = #{cameraId}
        </if>
        <if test="eventType != null and eventType != ''">
            and event_type = #{eventType}
        </if>
        <if test="status != null and status != ''">
            and status = #{status}
        </if>
        <if test="severity != null and severity != ''">
            and severity = #{severity}
        </if>
        <if test="areaId != null">
            and area_id = #{areaId}
        </if>
        <if test="occurredFrom != null">
            and event_time &gt;= #{occurredFrom}
        </if>
        <if test="occurredTo != null">
            and event_time &lt;= #{occurredTo}
        </if>
    </sql>

    <select id="countByTenant" resultType="long">
        select count(1) from video_ai_event
        <where>
            <include refid="eventWhere"/>
        </where>
    </select>

    <select id="listByTenant" resultType="com.fgroupboss.ai.psm.video.model.entity.VideoAiEventEntity">
        select * from video_ai_event
        <where>
            <include refid="eventWhere"/>
        </where>
        order by event_time desc, id desc
        limit #{offset}, #{limit}
    </select>

    <select id="findRecentByDedupKey" resultType="com.fgroupboss.ai.psm.video.model.entity.VideoAiEventEntity">
        select * from video_ai_event
        where tenant_id = #{tenantId}
          and dedup_key = #{dedupKey}
          and deleted = 0
          and event_time &gt;= #{since}
        order by event_time desc
        limit 1
    </select>
</mapper>
