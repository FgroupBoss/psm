<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.fgroupboss.ai</groupId>
    <artifactId>psm-platform-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>psm-platform-backend</name>
    <description>PSM safety management platform backend scaffold</description>

    <modules>
        <!-- ============================================================ -->
        <!-- Layer 0: BOM — 版本管理中心                                  -->
        <!-- ============================================================ -->
        <module>bom/psm-common-bom</module>

        <!-- ============================================================ -->
        <!-- Layer 1: Common 基础设施（无跨域依赖）                       -->
        <!-- ============================================================ -->
        <module>common/psm-common-core</module>
        <module>common/psm-common-data</module>
        <module>common/psm-common-web</module>
        <module>common/psm-common-feign</module>
        <module>common/psm-common-security</module>

        <!-- ============================================================ -->
        <!-- Layer 2: 领域服务（6 个完整 Spring Boot 服务）               -->
        <!--   无跨域 compile 依赖，跨域调用走运行时 HTTP                 -->
        <!--   identity        — 身份与接入（认证/授权/审计/文件/通知）   -->
        <!--   operation       — 作业管控（许可/承包商/移动BFF）          -->
        <!--   realtime        — 实时感知（告警/定位/视频AI）             -->
        <!--   risk            — 风险防控（危险源/巡检/双防）             -->
        <!--   process-safety  — 过程安全（PHA/MOC/PSSR/屏障）           -->
        <!--   incident-gov    — 事件治理（事故/CAPA/报表/监管上报）      -->
        <!-- ============================================================ -->
        <module>apis/psm-identity-api</module>
        <module>apis/psm-process-safety-api</module>
        <module>apis/psm-incident-governance-api</module>
        <module>apis/psm-risk-api</module>
        <module>apis/psm-realtime-api</module>
        <module>apis/psm-operation-api</module>
    </modules>

    <properties>
        <java.version>1.8</java.version>
        <spring-boot.version>2.7.18</spring-boot.version>
        <lombok.version>1.18.30</lombok.version>
        <mybatis-plus.version>3.5.7</mybatis-plus.version>
        <psm.common.version>1.0.0-SNAPSHOT</psm.common.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
